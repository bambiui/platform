# Archived Apps

Everything under `apps/_archived` is inactive reference material only. These apps are not current workspace, build, registry, or CLI targets.

Do not use old `@bambiui/components`, `@bambiui/tokens`, docs, studio, or archived marketing references as guidance for the active architecture. Current source of truth is the DOM Protocol, React-only, CLI-first flow in `packages/core`, `packages/generator`, `packages/registry`, `packages/cli`, and `apps/www`.
