{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bambiui.com/registry.schema.json",
  "title": "bambiui registry v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "styles", "components"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "integer",
      "const": 2
    },
    "styles": {
      "type": "object",
      "additionalProperties": false,
      "required": ["global"],
      "properties": {
        "global": {
          "$ref": "#/$defs/registryPath"
        }
      }
    },
    "components": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/component"
      }
    }
  },
  "$defs": {
    "component": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "contract", "controller", "style", "files", "exports"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "contract": {
          "$ref": "#/$defs/registryPath"
        },
        "contractFiles": {
          "$ref": "#/$defs/frameworkFiles"
        },
        "controller": {
          "$ref": "#/$defs/registryPath"
        },
        "style": {
          "$ref": "#/$defs/registryPath"
        },
        "adapter": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "vue": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "svelte": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "solid": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "html": {
              "$ref": "#/$defs/frameworkFiles"
            }
          }
        },
        "adapters": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": {
              "$ref": "#/$defs/adapterMetadata"
            },
            "vue": {
              "$ref": "#/$defs/adapterMetadata"
            },
            "svelte": {
              "$ref": "#/$defs/adapterMetadata"
            },
            "solid": {
              "$ref": "#/$defs/adapterMetadata"
            },
            "html": {
              "$ref": "#/$defs/adapterMetadata"
            }
          }
        },
        "files": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "vue": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "svelte": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "solid": {
              "$ref": "#/$defs/frameworkFiles"
            },
            "html": {
              "$ref": "#/$defs/frameworkFiles"
            }
          }
        },
        "exports": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": { "$ref": "#/$defs/exportNames" },
            "vue": { "$ref": "#/$defs/exportNames" },
            "svelte": { "$ref": "#/$defs/exportNames" },
            "solid": { "$ref": "#/$defs/exportNames" },
            "html": { "$ref": "#/$defs/exportNames" }
          }
        }
      }
    },
    "frameworkFiles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/registryPath"
      }
    },
    "adapterMetadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "mode"],
      "properties": {
        "status": {
          "type": "string",
          "enum": ["active", "legacy"]
        },
        "mode": {
          "type": "string",
          "enum": ["generic", "frozen"]
        }
      }
    },
    "exportNames": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "registryPath": {
      "type": "string",
      "minLength": 1
    }
  }
}
