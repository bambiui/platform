---
title: Theme Builder
description: Generate a custom Bambi UI theme and copy the CSS override.
---

import ThemeBuilder from '../../components/ThemeBuilder';

Pick a primary hue, chroma, border radius, and preview mode. Copy the generated CSS into your app's stylesheet.

<ThemeBuilder client:load />
