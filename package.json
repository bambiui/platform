{
  "name": "@bambiui/workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:static": "node scripts/build-static.mjs",
    "check": "pnpm check-types && pnpm check-registry && pnpm --filter @bambiui/core test:tabs && pnpm --filter @bambiui/core test:roving-focus && pnpm --filter bambiui smoke",
    "check-registry": "node scripts/check-registry.mjs",
    "registry:refresh": "node scripts/registry-refresh.mjs",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "smoke:templates": "node scripts/smoke-templates.mjs",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.9",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22.12.0"
  }
}
