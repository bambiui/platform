{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bambiui.com/registry.schema.json",
  "title": "bambiui registry v2",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "styles", "components"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "integer",
      "const": 2
    },
    "styles": {
      "type": "object",
      "additionalProperties": false,
      "required": ["global"],
      "properties": {
        "global": {
          "$ref": "#/$defs/registryPath"
        }
      }
    },
    "components": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/$defs/component"
      }
    }
  },
  "$defs": {
    "component": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "contract", "controller", "style", "files", "exports"],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "contract": {
          "$ref": "#/$defs/registryPath"
        },
        "contractFiles": {
          "$ref": "#/$defs/registryFileList",
          "description": "Shared contract helper files copied next to the component contract."
        },
        "primitiveFiles": {
          "$ref": "#/$defs/registryFileList",
          "description": "Optional explicit list of shared primitive source files copied into component/primitives/. Transitive primitive dependencies must be listed explicitly."
        },
        "controller": {
          "$ref": "#/$defs/registryPath"
        },
        "style": {
          "$ref": "#/$defs/registryPath"
        },
        "adapter": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": {
              "$ref": "#/$defs/registryFileList"
            }
          }
        },
        "adapters": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": {
              "$ref": "#/$defs/adapterMetadata"
            }
          }
        },
        "files": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": {
              "$ref": "#/$defs/registryFileList"
            }
          }
        },
        "exports": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "react": { "$ref": "#/$defs/exportNames" }
          }
        }
      }
    },
    "registryFileList": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/registryPath"
      }
    },
    "adapterMetadata": {
      "type": "object",
      "additionalProperties": false,
      "required": ["status", "mode"],
      "properties": {
        "status": {
          "type": "string",
          "enum": ["active"]
        },
        "mode": {
          "type": "string",
          "enum": ["generic"]
        }
      }
    },
    "exportNames": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    },
    "registryPath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.(?:/|$)).+"
    }
  }
}
