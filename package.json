{
  "name": "@bambiui/workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check": "pnpm check-types && pnpm check-registry && pnpm --filter bambiui smoke",
    "check-registry": "node scripts/check-registry.mjs",
    "deploy-static": "turbo run build --filter=docs --filter=builder && mkdir -p apps/docs/dist/builder && cp -r apps/builder/dist/. apps/docs/dist/builder/ && node scripts/copy-registry-assets.mjs",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.9",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22.12.0"
  }
}
